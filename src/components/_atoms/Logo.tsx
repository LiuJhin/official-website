import React from 'react';
import logo from '@/assets/logo.png'; // 替换为你的Logo图片路径
// import './Logo.css'; // 导入样式

const Logo = () => {
  return <img src={logo} alt="Logo" className="logo" />;
};

export default Logo;